import { createServerFn } from "@tanstack/react-start";
import { requireSupabaseAuth } from "@/integrations/supabase/auth-middleware";
import { z } from "zod";

type Msg = { role: "user" | "assistant" | "system"; content: string };

export const askAssistant = createServerFn({ method: "POST" })
  .middleware([requireSupabaseAuth])
  .inputValidator((data: { messages: Msg[]; context?: string }) =>
    z
      .object({
        messages: z
          .array(
            z.object({
              role: z.enum(["user", "assistant"]),
              content: z.string().min(1).max(4000),
            }),
          )
          .min(1)
          .max(40),
        context: z.string().max(8000).optional(),
      })
      .parse(data),
  )
  .handler(async ({ data }) => {
    const apiKey = process.env.LOVABLE_API_KEY;
    if (!apiKey) {
      return { ok: false as const, error: "LOVABLE_API_KEY غير مهيأ" };
    }
    const sys = [
      "أنت «مساعد يسير» — مساعد ذكي خبير داخل منصة يسير لإدارة المشاريع والمهام والعقود والحجوزات والملفات والتقويم والمذكرات والأعضاء والصلاحيات.",
      "أجب دائماً بالعربية الفصحى المبسطة، بأسلوب موجز ومباشر، واستخدم النقاط والعناوين عند الحاجة لتنظيم الإجابة. لا تذكر اسم مزوّد النموذج.",
      "",
      "## معرفتك بالمنصة (يسير)",
      "- المشاريع: لكل مشروع بيانات عقد (تاريخ بداية/نهاية، قيمة العقد، الدفعات مدفوع/مستحق، المسؤول من الشركة، الموظف المُكلَّف).",
      "- المهام: جدول مرن بأعمدة (اسم المهمة، المنصة، المستفيد، رقم المستند، تاريخ الإنجاز، تاريخ الانتهاء، الحالة، الأهمية، عدّاد تنازلي، مرفق التقرير النهائي).",
      "- مجموعات المهام تُضاف داخل المشروع نفسه وفي نهاية القائمة، ويمكن إضافة مهام لها.",
      "- الملفات والمذكرات والتقويم والحجوزات وخدمات الكتاب والمحادثة الداخلية.",
      "- الصلاحيات: للأدمن صلاحيات كاملة، وللموظف صلاحيات محددة حسب ما يفعّله الأدمن.",
      "- استخدم بيانات السياق الحقيقية أدناه للإجابة بدقّة، ولا تخترع أرقاماً أو أسماء غير موجودة.",
      "",
      "## خبرتك بالمنصات الحكومية السعودية (تعامل بدقة وحدّث المستخدم على آخر مسار معروف لديك)",
      "أنت ملمّ بإجراءات وخدمات المنصات التالية وتعرف الخدمات الرئيسية في كل منها وكيفية تنفيذها خطوة بخطوة:",
      "- أبشر (Absher) وأبشر أعمال: الهوية، التفويضات، تجديد الإقامة، نقل الكفالة، رخص القيادة.",
      "- نفاذ الوطني الموحّد وتوكلنا: الدخول الموحّد والتوقيع الرقمي.",
      "- منصة قوى ومدد ومساند والتأمينات الاجتماعية (GOSI): العقود، الأجور، الاشتراكات، نقل خدمات العمالة.",
      "- منصة بلدي ومنصة المنشآت ووزارة التجارة (السجل التجاري) ومركز الأعمال السعودي: التراخيص البلدية، السجلات التجارية، الأسماء التجارية، العلامات.",
      "- الزكاة والضريبة والجمارك (ZATCA): التسجيل، الإقرارات، الفوترة الإلكترونية (فاتورة) وضريبة القيمة المضافة 15%.",
      "- اعتماد ومنافسات وإيتمار وفُرص: المنافسات والمشتريات الحكومية وتأهيل الموردين.",
      "- ناجز ومعروض ووزارة العدل: الصكوك، التوكيلات، الإنذارات، التنفيذ، عقود الإيجار (إيجار).",
      "- إيجار، سكني، إجارة، البنك العقاري، وزارة الإسكان.",
      "- صحة ومواعيد ووصفتي ووزارة الصحة، مقيم، مساند.",
      "- منصة الأعمال (Saudi Business Center)، هيئة المحتوى المرئي، هيئة الاتصالات (CST)، سدايا.",
      "- الجوازات، المرور، الدفاع المدني (سلامة)، أرامكو، STC Business، حساب المواطن، الموارد البشرية.",
      "",
      "## معرفتك بالأنظمة والقوانين السعودية",
      "أنت على اطلاع جيد بـ: نظام العمل ولائحته التنفيذية، نظام الشركات، نظام مكافحة التستر التجاري، نظام المنافسات والمشتريات الحكومية ولائحته، نظام مكافحة الاحتيال المالي، نظام حماية البيانات الشخصية (PDPL)، نظام مكافحة الجرائم المعلوماتية، نظام التجارة الإلكترونية، أنظمة هيئة الزكاة والضريبة والجمارك (VAT، الفاتورة الإلكترونية، ضريبة الاستقطاع)، نظام الإيجار ونظام التنفيذ، نظام مكافحة غسل الأموال.",
      "وضّح دائماً للمستخدم أن إجاباتك القانونية إرشادية فقط وليست استشارة قانونية رسمية، وعند الحاجة وجّهه للجهة المختصة.",
      "",
      "## أسلوب الإجابة",
      "1) افهم سؤال المستخدم بدقّة. 2) إن كان متعلقاً بالمنصة استخدم سياقه الفعلي. 3) إن كان متعلقاً بإجراء حكومي أعطه: الجهة، رابط/مسار الوصول داخل المنصة المعنية، المتطلبات، الخطوات بالترتيب، الرسوم/المدة إن عرفتها. 4) إن لم تكن متأكداً قل ذلك بصراحة واقترح أفضل مرجع.",
      data.context ? `\n## سياق المستخدم الحالي\n${data.context}` : "",
    ].join("\n");

    try {
      const res = await fetch("https://ai.gateway.lovable.dev/v1/chat/completions", {
        method: "POST",
        headers: {
          Authorization: `Bearer ${apiKey}`,
          "Content-Type": "application/json",
        },
        body: JSON.stringify({
          model: "google/gemini-2.5-flash",
          messages: [{ role: "system", content: sys }, ...data.messages],
        }),
      });
      if (res.status === 429) return { ok: false as const, error: "تم تجاوز حد الاستخدام، حاول لاحقاً." };
      if (res.status === 402) return { ok: false as const, error: "نفد الرصيد، يرجى إضافة رصيد لمساحة العمل." };
      if (!res.ok) {
        const t = await res.text();
        return { ok: false as const, error: `خطأ ${res.status}: ${t.slice(0, 200)}` };
      }
      const json = await res.json();
      const reply = json?.choices?.[0]?.message?.content ?? "";
      return { ok: true as const, reply };
    } catch (e) {
      return { ok: false as const, error: e instanceof Error ? e.message : "خطأ غير معروف" };
    }
  });